name: Validate team.json

on:
  push:
    branches: [main]
    paths: ["team.json", "schemas/team.schema.json"]
  pull_request:
    paths: ["team.json", "schemas/team.schema.json"]

jobs:
  schema-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install ajv-cli
        run: npm install --global ajv-cli ajv-formats
      - name: Validate team.json
        run: ajv validate -c ajv-formats -s schemas/team.schema.json -d team.json --spec=draft2020 --strict=false
